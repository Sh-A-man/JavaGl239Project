package problem;

import javax.media.opengl.GL;
import javax.media.opengl.GL2;
import javax.media.opengl.GL2GL3;
import java.util.Vector;

public class Parabola {
    Vector2 F;
    Vector2 x0;

    public Parabola(Vector2 F, Vector2 x0) {
        this.F = F;
        this.x0 = x0;
        
    }

    public static void renderParabola(GL2 gl, Vector2 F, Vector2 x0, boolean field) { // F-это фокус, x0-вершина
        if (field) {
        Vector2 posF;
        gl.glBegin(GL.GL_LINE_STRIP);
        gl.glVertex2d(pos.x, pos.y);
        int n = 3600;
        for (int i =1; i<n; i++) {
            double
        }

        int a;
        int b;
        int c;

    }

}
