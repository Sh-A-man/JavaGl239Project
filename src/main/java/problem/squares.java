package problem;

    public class squares {

        int k;
        int N = k*(k-1)/2;

        public void addRandomPoints(int k) {
            for (int i = 1, i< N, i++;{
                Point p1 = Point.getRandomPoint();
                F.x = p1.x;
                F.y = p1.y;
                v0.x = p1.x;
                Point p2 = Point.getRandomPoint();
                v0.y = p2.y;




            }




            }

    // y = ax^2 + bx +c
}

